# This is a basic workflow to help you get started with Actions

name: Testing Multiple Repos

# Required inputs
inputs:
  customer_short_name:  # id of input
    description: 'Short text string that uniquely idendifies the customer.'
    required: true

# Controls when the action will run. 
on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - cboroson-patch-1
  pull_request:
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      # Checks-out a separate repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
        with:
          repository: cboroson/configuration
          path: 'configuration'
          token: ${{ secrets.GITHUB_TOKEN }}

      # Runs a single command using the runners shell
      - name: Show environment vars
        run: set
        
      - name: Show current working directory
        run: pwd

      - name: list directory
        run: ls -la "$GITHUB_WORKSPACE/configuration"
        
      - name: cat file
        run: cat "$GITHUB_WORKSPACE/configuration/$INPUTS_CUSTOMER_SHORT_NAME.json"

